<svg width="100%" height="100%" viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg"><script src="t.js">
const opt = {
  mode: 'same-origin',
  cache: 'only-if-cached',
}
onmessage = function(e) {
  const url = e.data
  console.log('url:', url)

  fetch(url, opt).then(res => {
    parent.postMessage(res.status === 200)
  }).catch(err => {
    parent.postMessage(false)
  })
}
</script></svg>
